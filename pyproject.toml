[project]
name = "tp-coo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "medrgr", email = "regraguimohammedamine2004@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
]

[project.scripts]
tp-coo = "tp_coo:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = [
    "tp_coo",
]
